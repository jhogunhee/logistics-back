-- 외부 문서 인용을 DB 코멘트에서 제거 (2026-08-03)
--   전제: 지금까지의 마이그레이션이 적용된 라이브 DB.
--
--   배경 —
--   설계 자료 일부가 이 레포에서 관리되지 않게 되면서, 그 문서를 경로·절 번호로 가리키던
--   인용이 전부 죽은 참조가 됐다. 소스·문서에서 걷어낸 것과 짝을 맞춰 DB 코멘트에서도 뺀다.
--   구조를 설명하던 자리는 「그 구조의 주인이 누구인가」로 바꿔 적었다 — 어차피 JSONB 구조의
--   실질적인 주인은 직렬화하는 DTO 쪽이다.
--
--   코멘트만 바꾼다. 테이블·컬럼·제약은 손대지 않으므로 몇 번을 돌려도 안전하다.
--
--   실행(DBeaver): 이 파일을 열고 Alt+X (Execute script).
-- =====================================================================

DO $doc_refs$
BEGIN
    COMMENT ON COLUMN stgy_rvsn.snpsht IS '정의 전체(헤더+하위 구성)의 JSON 직렬화. 직렬화 형태의 주인은 저장 서비스이고, 읽을 때도 같은 DTO로 역직렬화한다';

    COMMENT ON COLUMN stgy_exec_log.dcsn_trc IS '건별 판정 상세(라인×규칙 / 단계×후보). 관리 화면의 "왜 차단/배정됐나" 표의 원본이고, 구조는 판정 결과 DTO가 정의한다';

    COMMENT ON TABLE ptawy_stgy IS '적치 전략 헤더. 선택 기준은 발주구분 하나(유형당 1개 — ux_ptawy_stgy_odr_dvsn). 1차에서 전략은 추천만 한다 — 실행은 기존 즉시 MOVE 흐름 유지';

    RAISE NOTICE 'DB 코멘트 3건에서 외부 문서 인용 제거';
END
$doc_refs$;

-- =====================================================================
-- 적용 후 확인 (남아 있으면 0건이 아니다)
--   SELECT count(*) FROM pg_description WHERE description LIKE '%docs/%';
-- =====================================================================
